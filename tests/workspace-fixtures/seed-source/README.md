Workspace-config template fixture for seed files tests.

The installer is invoked with `--workspace-config` pointing to this folder's
`workspace.config.json`, and it should copy the `seedFiles[]` into the destination
workspace.

