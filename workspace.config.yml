# yaml-language-server: $schema=./defaults/schema/workspace-config.schema.json

version: "0.1.0"

# This is the DevDuck repository itself, so devduck_path points to current directory
devduck_path: "."

# Extend from the baseline install configuration
extends:
  - "devduck:defaults/workspace.install.yml"

modules:
  - core
  - cursor

moduleSettings: {}

repos: []
projects: []
checks: []
env: []
