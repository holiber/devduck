version: '0.1.0'

# Treat DevDuck workspace config as a layered document.
# This repo is the DevDuck root, so `devduck_path` is "." here.
devduck_path: .

extends:
  - devduck:defaults/workspace.install.yml

