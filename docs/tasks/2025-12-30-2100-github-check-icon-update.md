# GitHub check icon update

Add the barducks logo as an icon next to the GitHub "tests" check badge in `README.md`.

GitHub's Checks UI does not support custom images for GitHub Actions checks, so the icon was added to the `README.md` badge line instead.
