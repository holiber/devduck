## Summary

Make contributor rules easier for Cursor/agents to follow:

- Add root `.cursorrules` with short PR requirements (points to `CONTRIBUTING.md`).
- Add `.github/pull_request_template.md` checklist.
- Remove duplicated rules file under `.cursor/` to avoid divergence.

