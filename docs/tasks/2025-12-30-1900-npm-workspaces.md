# npm workspaces + @barducks/* packages

## 0. Meta

- Date: 2025-12-30
- Stage: 0
- Area: monorepo / npm workspaces

## 1. Intake

Migrate the repo to npm workspaces, move extension metadata from `MODULE.md` frontmatter into per-extension `package.json`, and enable short imports for extensions via `@barducks/sdk`.

## 2. Status Log

- CHECKPOINT: Stage 0 pushed

