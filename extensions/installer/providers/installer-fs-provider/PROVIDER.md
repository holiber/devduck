---
name: installer-fs-provider
type: installer
description: Install entities from local filesystem (copy folder)
---
# installer-fs-provider

Installs from a local directory (`src`) into destination directory (`dest`) by copying files.

