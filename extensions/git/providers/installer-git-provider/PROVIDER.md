---
name: installer-git-provider
type: installer
description: Install git repositories into destination directory (git clone)
---
# installer-git-provider

Clones git repositories (`src`) into `dest`.

