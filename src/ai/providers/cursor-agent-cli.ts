/**
 * cursor-agent-cli provider (scaffolding).
 *
 * Real implementation should:
 * - run cursor-agent-cli (or Cursor CLI integration)
 * - return model output + usage stats (tokens/cost) when available
 */

export async function run(): Promise<never> {
  throw new Error('cursor-agent-cli provider is not implemented yet (scaffolding only)');
}

